# Element Drift — 引き継ぎ書 (2026-07-07)

次セッションの Claude 向け。この文書だけで文脈が復元できるように書いてある。
読んだらまず `max/elements.js` と `max/README.md` に目を通すこと。

## 作品コンセプト (変わらない核)

三好賢聖の「運動共感エレメント」(傾き・回転・関節・張力・触覚・加速・減速・重力・
振動・リズム・体積・出入り・自己受容・静止・空間) を土台に、
**音のマッピングを駆動するエレメントが移ろう (スポットライト) システム**を身体に纏う
メディアアート。スポットライトが当たっている要素を身体で表出しないと音が鳴らない/痩せる
ため、踊り手は「普段しない動きを自然としてしまう」。これが作品の狙い。

- 参照テキスト: 三好の著書の該当章 (ユーザーが持っている。第一〜第三のレンズ)
- 旧プロトタイプ: `element_drift_v2.html` (Web Audio + DeviceMotion 単一HTML)。仕様は
  ユーザー提供の仕様書にあるが、現在は Max 版が大きく先行しており参照不要

## 確定した方針 (ユーザー決定事項 — 蒸し返さない)

- プラットフォーム: **Max/MSP + ZIG SIM PRO** (iPhone から OSC/UDP、ポート **9600**)
- ロジックは **Max 内 [js] (ES5!) に集約**。パッチは配線のみ。人手デバッグ最小化が最優先
- センサーは受信レートそのまま取り込み (ダウンサンプリングしない)。特徴計算・出力は 20Hz
- キャリブレーション: インスタレーション形式 → 走行正規化 (減衰つき min/max、半減期≈5分)。
  冒頭の儀式なし
- ブレンド: 最初は1要素、いずれ 2-3 要素同時
- Phase 3 の音響設計 (合意済み): **万能シンセ1台のパラメータ群 p に、
  p = p_base + M · (w ∘ a) で写像**。M は 10要素×パラメータの行列で coll/dict の
  データとして持つ (パッチを触らずチューニング、将来は ML 写像に差し替え可能)。
  w = スポットライト重み (今は one-hot、後に top-2〜3 softmax)、a = 活性度
- 将来アイデア: ML で「飽き」対策 (未着手で良い)

## フェーズ計画と現在地

1. ✅ Phase 1: ZIG SIM 受信 → 特徴抽出 → 10要素活性の可視化
2. ✅ Phase 2: 特徴抽出の実機チューニング (完了。全10要素がユーザーの身体感覚と一致確認済み)
3. ✅ Phase 3: 音響 — **行列写像は廃止し「1要素=1ボイスパッチ」に転換** (2026-07-07)。
   js は制御ストリーム (ed-acts / ed-weights / ed-events + gain) のみ送り、音の設計は
   `max/elem.<名前>~.maxpat` ×10 でユーザーがパッチングで行う。設計原理は Godøy の
   3類型 (impulsive/sustained/iterative) を要素の運動プロファイルに写すこと。
   「招待」(迷子時にターゲットの音が先に身振りして見せる) は保留 — 様子を見て追加可
4. ✅→▶ Phase 4: 離散ドリフトは「連続性がない・マルコフ的」というフィードバックで廃止し、
   **追跡方策 (段階A) に置換 — 実装済み・実機検証待ち**。獲物 prey = 10要素単体上の
   連続な点がダンサーの活性プロファイルから逃げ (近いほど速く) つつ低慣れ要素へ向かう。
   gain = closeness の連続勾配 (下限 GAIN_FLOOR=0.08)。「何かを追いかけている体感」が狙い。
   手動 spotlight 0-9 は従来通り (音作り用)。`logging 1` で学習データ収集可
5. 段階B (ML、**NeurIPS Creative AI Track 2027 が野心**): 獲物の逃げる地図を学習に置換。
   候補は予測誤差型 (ダンサーの次動作を予測する小モデルの「読める方向」から逃げる、推し) と
   潜在空間型 (VAE の低密度領域へ)。Python サイドカー + OSC を想定 (js集約方針をここで初めて破る、ユーザー了解済みの方向)。
   評価は 固定/ランダムドリフト/追跡 の条件比較 (novelty 指標は実装済み)。締切は例年5-6月 → 2027サイクル対象
6. Phase 5: 新要素 (hapticity=マイク、space/volume=ARKit)

## ファイル構成

- `max/ElementDrift.maxpat` — メインパッチ: udpreceive 9600 → js → 制御分配
  (s ed-acts/ed-weights/ed-events) → ボイス10個の合流 → gain → リバーブ。
  パッチ類は `tools/genpatch.js` 相当のスクリプトで生成した (手書きでない)
- `max/elem.<名前>~.maxpat` ×10 — 要素ボイス (音の設計はユーザーがここで)
- `max/elements.js` — 全制御ロジック (ES5)。特徴抽出、正規化、ドリフト、重み、
  イベント、セルフテスト。音は決めない
- `max/mapping.json` — reach 行列 (ドリフトの近さ) のみ
- `max/README.md` — ZIG SIM 設定、使い方、ボイスの契約、設計原理 (説明はここが最新)
- `tools/harness.js` — node 検証ハーネス (恒久化済み)

## 検証の仕組み (重要 — 変更したら必ず回す)

- **セルフテスト**: パッチで `selftest 1`。合成センサーデータで 11 セグメント
  (各5秒、1周55秒) を巡回し、2周目からセグメントごとに期待要素が top-3 に入るか
  自動採点して print する (`check <名前> PASS/FAIL top3: ...`)
- **node ハーネス**: Max なしで同じ検証が回る。
  スクラッチパッドに置いたが消えている可能性が高い。無ければ再作成する:
  Max のグローバル (Task, outlet, arrayfromargs, messagename, Date.now モック) をスタブし、
  eval で elements.js を読み、selftest(1) して 16ms 刻みで selfstep() + 50ms毎 update() を
  180秒分回し、check 行の FAIL を数えるだけの ~40 行のスクリプト
- **教訓**: シミュだけ通って実機で死ぬことが繰り返し起きた。実機で失敗した動きは
  必ずセルフテストのセグメントとして追加してある (平面スピン、円弧→停止、微細な震え等)。
  この習慣を続けること

## 10要素の現在の実装 (実機フィードバック反映済み)

インデックス順: balance, rotation, articulation, acceleration, deceleration, gravity,
vibration, rhythm, tension, stillness

| 要素 | 実装 | 実機チューニングの経緯 |
|---|---|---|
| 傾き | tilt/π そのもの (gravity ベクトルから) | 動きによる抑制項は削除 (ユーザー指示) |
| 回転 | **自転と公転の max**。自転=rotRMS×軸安定×方向持続×(0.7+0.3遠心相関)。公転=a×ȧ の一貫性から角速度推定×0.5 | 遠心力を乗数にすると半径0の平面スピンが死ぬ→ボーナス化。姿勢固定で腕が円周を回る動き(公転)はジャイロ0なので加速度から検出。公転推定はノイズで暴れる→振幅ゲート(|a|²>1)・一貫性下限(0.4)・上限(15rad/s)で頑健化 |
| 関節 | **円弧への確信の漸増**: 掃引角に応じ徐々に上昇(上限0.6)、停止で完結ボーナス(+0.4×鋭さ)、1秒超回り続けると回転とみなし減衰。ヒステリシス+ピーク比20%の停止判定 | 反転カウント→イベント検出→漸増モデルと3回作り直した。「突然爆発する」がNGで「円弧だと分かってきたら徐々に」がユーザーの知覚モデル。スピン序盤に一瞬立つのは仕様(曖昧さの表現) |
| 加速/減速 | 平滑化した\|a\|包絡線(150ms MA)の増分/減分の平均 | 生Δだと震えに乗っ取られるため包絡線化 |
| 重力 | 鉛直成分RMS × 動きの鉛直性(vertRMS/linRMS) | 「落下がマックス」でなく「重力方向への移動/抗う移動」を等しく計る(ユーザー指示)。自由落下は鉛直9.8で自然に最大級 |
| 振動 | スペクトル高域 (下記) | |
| リズム | slow バッファ(10Hz,3s)の自己相関ピーク − ノイズ床0.5 ×2 | 短系列の自己相関はノイズでも~0.5出るため下駄を引く |
| 張力 | スペクトル低域 + 高域×TREMOR_GAIN(14)×max(0,1−spec.rms×0.35) | 「震えを信じる」方向に2回増強。抑制はスペクトルと同じ1.5s窓のRMSで(500ms窓だと激しい動きの直後に誤発火) |
| 静止 | max(0, 1−(linRMS+rotRMS)×0.8) | 変更なし |

- **帯域分離**: 加速度(符号つき・軸別! 大きさだと整流で周波数2倍に化ける)の1.5s窓を
  Goertzel で分解、4.5Hz中心のゆるやかなシグモイド(幅1.5)で低域=張力/高域=振動に按分
- **出力段**: 非対称スムージング (アタック0.12s / リリース0.5s)。
  「すぐ反応するが、その後のバタつきは滑らか」というユーザー要望
- **正規化**: 相対値。判定は絶対値でなく「どの要素が支配的か」で行う

## 主な調整ノブ (elements.js 冒頭)

`SPLIT_HZ/SPLIT_W` (帯域交差), `TREMOR_GAIN/TREMOR_SUPP` (張力),
`ARC_ON/ARC_OFF/MIN_ARC/ARC_FULL/ARC_DECAY` (関節), `ATTACK_TAU/RELEASE_TAU` (出力),
`NORM_DECAY` (正規化半減期)

## 既知の性質 (バグではない、ユーザー了解済み or 未議論)

- rhythm は周期的な動き全般に併発する (概念的に正しい)
- 鉛直バウンスで gravity と tension が併発 (「重さに抗う緊張」として自然、要観察)
- 公転 (腕で円を描く) で tension が併発 (~0.65)
- スピン序盤に articulation が一瞬立つ (知覚の曖昧さの表現として意図的に残した)
- accel/decel は対で動く (同じ包絡線由来、慣性感として妥当)

## Phase 3 に着手するときの指針

- 万能シンセのパラメータ候補 (~10次元): ピッチ、FMインデックス/倍音性、フィルタcutoff、
  レゾナンス、AM速度、AM深さ、ノイズ混合、デチューン/うなり、リバーブ量
- 行列 M は coll か dict でロード。1要素=1行。まず one-hot スポットライトで
  v2 の対応 (傾き=ピッチ+うなり、振動=ノイズ+高速AM、静止=純音、等) を行列の初期値に
- master gain ∝ アクティブ要素の活性度 (表出しないと鳴らない) は作品の心臓部、必ず維持
- スポットライト遷移はスムージング済み活性がそのままクロスフェードに使える
- 実装は同じく js 中心 + 音は Max シグナル系。poly~ でなく1台のシンセに行列写像が合意事項

## ユーザーとの働き方

- 日本語。まず理解と指針を示し、フィードバックをもらってから実装するループ
- 概念 (三好のテキスト) への忠実さを重視しつつ、最終判断は身体で試した感触。
  「〜がワークしない」と言われたら、実機とシミュの差 (ノイズ、ゼロ交差の速度、
  正規化レンジの汚染など) をまず疑うと当たりが多い
- 実装後は必ず node ハーネスで ALL PASS を確認してから報告する
